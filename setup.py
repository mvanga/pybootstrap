from distutils.core import setup
setup(
  name = 'py-bootstrap',
  packages = ['py-bootstrap'], # this must be the same as the name above
  version = '0.1',
  description = 'A random test lib',
  author = 'Manohar Vanga',
  author_email = 'manohar.vanga@gmail.com',
  url = 'https://github.com/mvanga/py-bootstrap', # use the URL to the github repo
  download_url = 'https://github.com/mvanga/py-bootstrap/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['statistics', 'bootstrap', 'confidence'], # arbitrary keywords
  classifiers = [],
)
